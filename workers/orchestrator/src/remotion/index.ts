// Remotion bundle entry — imported by `@remotion/bundler` in dev-render.ts
// and by Remotion's CLI during production renders.
import { registerRoot } from 'remotion';
import { RemotionRoot } from './Root.js';

registerRoot(RemotionRoot);
